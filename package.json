{
  "name": "csharp",
  "version": "1.0.0",
  "description": "JavaScript learning path documentation",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "npm run docs:build && firebase deploy --only hosting:csharp",
    "deploy:preview": "npm run docs:build && firebase hosting:channel:deploy preview --only hosting:csharp"
  },
  "keywords": [
    "javascript",
    "documentation",
    "tutorial"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "^1.0.0-rc.20",
    "vue": "^3.3.4"
  }
}